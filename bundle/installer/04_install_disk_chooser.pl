# User selects which disk to install to here. Always erases entire disk.
# Detect other OSs and user data on disk and warn that they will be erased.
# Detect other OSs on system and warn that they won't be bootable(try to include name of OS in warning) (due to bootloader, but that's too advanced for the user)
